import React from 'react'
import { motion } from 'framer-motion'

export const LoadingSpinner = () => {
  return (
      <p className="mt-10 text-gray-600 text-center text-sm font-medium">Generating hashes...</p>
  )
}
